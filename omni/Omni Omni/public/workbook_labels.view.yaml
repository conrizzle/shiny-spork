# Reference this view as workbook_labels
schema: public

dimensions:
  name: {}
  created_at: {}

  organization_id:
    format: ID

  user_id:
    sql: '"userId"'
    format: ID

  workbook_id:
    sql: '"workbookId"'
    format: ID

measures:
  count:
    aggregate_type: count
custom_compound_primary_key_sql:
  [
    "${workbook_labels.name}",
    "${workbook_labels.organization_id}",
    "${workbook_labels.workbook_id}"
  ]
