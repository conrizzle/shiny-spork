# Reference this view as folder_favorites
schema: public

dimensions:
  created_at: {}
  updated_at: {}

  organization_id:
    format: ID

  user_id:
    sql: '"userId"'
    format: ID

  folder_id:
    sql: '"folderId"'
    format: ID

measures:
  count:
    aggregate_type: count
custom_compound_primary_key_sql:
  [
    "${folder_favorites.organization_id}",
    "${folder_favorites.user_id}",
    "${folder_favorites.folder_id}"
  ]
