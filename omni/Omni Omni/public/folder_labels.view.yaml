# Reference this view as folder_labels
schema: public

dimensions:
  name: {}
  created_at: {}

  organization_id:
    format: ID

  user_id:
    sql: '"userId"'
    format: ID

  folder_id:
    sql: '"folderId"'
    format: ID

measures:
  count:
    aggregate_type: count
custom_compound_primary_key_sql:
  [
    "${folder_labels.name}",
    "${folder_labels.organization_id}",
    "${folder_labels.folder_id}"
  ]
