# Reference this view as organization_labels
schema: public

dimensions:
  name: {}
  created_at: {}

  organization_id:
    format: ID

  user_id:
    sql: '"userId"'
    format: ID

measures:
  count:
    aggregate_type: count
custom_compound_primary_key_sql: [ "${organization_labels.name}", "${organization_labels.organization_id}" ]
