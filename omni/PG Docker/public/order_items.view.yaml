# Reference this view as order_items
schema: public

dimensions:
  quantity:
    label: unrelated

  id:
    format: ID
    primary_key: true

  order_id:
    format: ID

  inventory_item_id:
    format: ID

measures:
  count:
    aggregate_type: count
