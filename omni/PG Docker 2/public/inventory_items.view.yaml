# Reference this view as inventory_items
schema: public

dimensions:
  created_at: {}
  sold_at: {}

  cost:
    label: costly

  id:
    format: ID
    primary_key: true

  product_id:
    format: ID

measures:
  count:
    aggregate_type: count
