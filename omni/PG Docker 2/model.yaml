access_grants:
  view_test:
    # subby 5
    user_attribute: foo_attr
    allowed_values: [ allowed ]

topics:
  # newww
  users:
    joins:
      orders: {}

    views:
      users:
        dimensions:
          age:
            label: Age Labeled In Topic
