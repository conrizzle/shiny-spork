# Reference this view as inventory_items
schema: public

dimensions:
  created_at: {}
  cost: {}

  sold_at:
    label: demo

  id:
    format: ID
    primary_key: true

  product_id:
    format: ID

measures:
  count:
    aggregate_type: count
